# JOSS paper status

This directory is a **draft only** for public `exactcis==1.1.2` at tag
`v1.1.2` (`f0cbf3c0ab37ea9a7f96e1b70367f40b6281f3c4`). It must not be submitted
until all of the following have been checked against the then-current JOSS
review criteria:

- the public repository has more than six months of public development
  history;
- the paper records concrete research impact beyond availability, testing, and
  packaging;
- the author affiliation placeholder has been resolved;
- the AI-assistance record identifies the exact tool/model versions used, or
  explains any unavailable historical version metadata;
- all references and rendered equations have been checked;
- every example still runs against the cited immutable release; and
- the author has approved external submission.

The JSS replication packet under `replication/` is the numerical authority for
the examples shared by the two papers. JOSS drafting does not change the
package API, dependencies, defaults, or release version.
